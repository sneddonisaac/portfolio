/** @type {import('next').NextConfig} */
module.exports = {
    reactStrictMode: true,
    images: {
        domains: ['media.graphassets.com', 'images.unsplash.com'],
    },
    i18n: {
        locales: ['en'],
        defaultLocale: 'en',
    },
}
